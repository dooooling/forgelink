# ForgeLink 开发规范

> 状态：初版。架构决策以 `Rust工业IoT采集平台架构设计方案.md` 的 Normative 章节为准。

## 1. 基本原则

- 优先实现 MVP，不提前实现路线图中的扩展协议和功能。
- 保持模块职责单一，禁止跨层绕过既定接口。
- 公共模型、配置和协议发生变化时，必须同步文档与测试。
- 不为假设中的需求增加抽象；重复且稳定后再提取公共组件。

## 2. 目录与依赖

- `crates/`：平台公共能力；`drivers/`：协议实现；`profiles/`：品牌/型号映射；`apps/`：可执行程序。
- `apps` 只负责组装和启动，不承载可复用业务逻辑。
- Driver 只能处理 Transport、协议和原始结果，不依赖 Profile、Domain 或北向模块。
- Profile Engine 负责原始值与语义值转换；Domain Model 负责标准领域语义。
- Core 不得依赖具体 Driver，也不得按 `driver_id` 编写条件分支。
- crate 之间禁止循环依赖；新增依赖前确认其必要性、维护状态、许可证和目标平台支持情况。

## 3. Rust 编码

- workspace 建立后统一 Rust edition、`rust-version` 和依赖版本。
- 类型使用 `UpperCamelCase`，函数和模块使用 `snake_case`，常量使用 `SCREAMING_SNAKE_CASE`。
- 生产路径禁止使用 `unwrap()`、`expect()`、`panic!()` 处理可恢复错误；测试代码可以合理使用。
- 错误必须保留上下文和错误源；不得仅返回无法定位的字符串。
- `unsafe` 仅限 FFI 或确有必要的底层代码，范围必须最小，并说明安全前提。
- 代码必须提供充分、准确且便于维护的注释，重点说明设计意图、边界条件、协议规则、并发约束和异常处理原因，禁止只复述代码行为。
- 所有公共模块、类型、字段、trait、函数和常量必须使用 Rustdoc（`//!`、`///`）编写文档。
- Rustdoc 应按需要包含 `# Examples`、`# Errors`、`# Panics`、`# Safety` 等章节；涉及单位、取值范围、时间语义、线程安全和内存所有权时必须明确说明。
- 优先使用 Rustdoc 内部链接引用相关类型和方法，避免复制容易失效的说明。
- FFI、`unsafe`、复杂状态机、协议编解码、批量优化和控制安全链路必须有详细注释，说明不变量与调用约束。
- 私有辅助代码按复杂度添加注释；显而易见的赋值、分支和循环无需逐行注释。
- 不保留无用代码、注释掉的旧实现和未说明的全局状态。

## 4. 模型与接口

- 复用设计文档中的规范模型和命名，不创建同义类型。
- Driver 地址始终视为不透明字符串，仅 Driver 可以解析。
- Driver 返回 `RawReadResult`、`RawEvent` 等原始结果；`Observation` 只能由 Profile + Domain 映射后生成。
- 每个 Observation 必须携带 Quality 和时间；错误结果不得伪装为正常值。
- Property Write 与 Command Execute 都必须经过 Control Engine。
- 公共 API、配置和消息必须显式版本化；破坏性变更不得静默发布。

## 5. 异步与并发

- 网络 I/O、设备并发采集、消息发布和后台调度应合理使用 Rust 异步机制，以提升吞吐并避免线程阻塞；纯 CPU 计算或短小同步逻辑不应为了形式统一而强行异步化。
- 异步运行时、任务模型和并发度必须结合协议特性与设备能力设计，不以无限制创建任务作为性能优化手段。
- 禁止在 Tokio 异步任务中直接执行阻塞 I/O；阻塞 SDK 使用专用 Worker 或 `spawn_blocking`。
- CPU 密集型工作使用受控的专用线程池或 `spawn_blocking`，不得长期占用异步运行时工作线程。
- 通道和队列默认有界，并明确容量、背压和溢出策略。
- 批量处理优先于逐点创建任务；并发请求使用 `Semaphore`、有界任务集等方式限制并发量。
- 每次设备请求必须有超时；单设备故障不得阻塞其他设备。
- 同一设备的读写并发必须服从 Driver Session Scheduler。
- 后台任务必须支持有序停止，不得遗留无法回收的任务或回调。
- 性能优化必须以指标和基准测试为依据，关注吞吐、P95/P99 延迟、CPU、内存、队列积压和任务数量。

## 6. 配置、日志与安全

- 配置在启动或下发时完成结构、范围和交叉字段校验，错误应指明字段路径。
- 日志统一通过 `diagnostics` crate 初始化（`init_logging`）：默认级别 `INFO`、text 格式；`RUST_LOG` 覆盖级别，`FORGELINK_LOG_FORMAT=text|json` 切换格式。初始化失败必须输出脱敏错误并以非零码退出；初始化幂等，重复调用不改变首次结果。
- 日志级别语义：`error`=组件无法继续运行；`warn`=可恢复失败（非法 Profile、连接重试等）；`info`=启动/关闭、Profile 加载完成等生命周期事件；`debug`=批次、调度与耗时明细；`trace`=低层诊断，默认关闭。
- 结构化字段规范：`component` 必填；按场景附加 `device_id`、`request_id`、`error_code`（稳定、机器可读编码）、`batch_size`、`elapsed_ms` 等；禁止动态拼接字段名。
- 敏感信息不得入日志：密码、Token、私钥、完整连接配置、完整控制载荷。错误信息可能携带敏感内容时用 `diagnostics::redact` 脱敏；脱敏是兜底手段，不能替代“不记录敏感字段”。
- 高频采集路径（如每条 Observation 组装）不得逐条打日志；错误返回调用方，由上层带设备 ID 统一记录。
- 控制能力默认关闭，只有认证、授权和传输安全配置完整后才能启用远程控制。

## 7. 测试与质量

- 新功能至少包含正常路径、边界条件和错误路径测试；修复缺陷必须增加回归测试。
- Profile 的读取转换与写入逆转换必须成对测试。
- Driver 必须覆盖地址校验、批量读取、超时、断线重连和错误映射。
- FFI 必须覆盖 ABI 版本、空指针/长度、内存释放、panic 隔离和回调生命周期。
- workspace 统一执行格式化、静态检查和测试：
  `cargo fmt --all --check`、`cargo check --workspace --all-targets --all-features`、
  `cargo test --workspace --all-features`、
  `cargo clippy --workspace --all-targets --all-features -- -D warnings`。
- Rustdoc 构建和文档测试纳入质量检查：`cargo doc --workspace --all-features --no-deps`；文档示例应可编译或明确标记为不可运行示例。
- 涉及平台相关代码时，至少检查 Windows x64、Linux x64、Linux ARM64 的兼容性。

## 8. 变更要求

- 变更应聚焦单一目标，避免夹带无关重构。
- 修改公共模型、分层边界、ABI 或北向契约前，先更新设计文档中的 Normative 章节。
- 新增设备时先判断应新增 Profile 还是 Driver，不复制已有协议实现。
- 提交前检查：代码、测试、配置示例和文档是否一致。

## 9. Git 与提交规范

- 禁止直接在 `main` 分支开发、提交或推送；`main` 只接受 Pull Request 合并。
- 每项工作使用独立分支，建议命名为 `feat/<name>`、`fix/<name>`、`docs/<name>` 或 `chore/<name>`。
- 一个提交只完成一个清晰目标，避免混入无关格式化或重构。
- 提交信息使用 Conventional Commits：
  - `feat:` 新功能
  - `fix:` 缺陷修复
  - `docs:` 文档变更
  - `refactor:` 重构
  - `test:` 测试变更
  - `build:` 构建或依赖变更
  - `ci:` CI 变更
  - `chore:` 其他维护
- 提交信息使用中文或英文均可，但必须清楚说明变更目标；推荐格式：`<type>: <summary>`。
- 提交前至少执行适用的格式化、构建、测试和 Rustdoc 检查；失败时不得提交未说明的结果。
- Pull Request 必须说明变更内容、验证结果和已知限制；涉及架构、公共 API、ABI 或北向协议时必须同步更新设计文档。
- 禁止提交 `target/`、密钥、Token、本地配置、编辑器缓存和生成的临时文件。

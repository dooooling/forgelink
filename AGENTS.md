# AGENTS.md

## 仓库状态

- 当前已完成：核心规范模型（`crates/observation-model`，§96 共享模型）、Driver 契约与 ABI v1 Tag/Envelope 契约（`crates/driver-sdk`，§17）、日志基础设施（`crates/diagnostics`，§6）、Native Plugin 加载器（`crates/driver-loader`，§19/§20）、Profile Engine（`crates/profile-engine`：Device Profile 模型 §37 + 运行逻辑——完整校验 validate、JSON 加载 loader、注册表 registry、读解码/写编码 convert §37.1）、Domain Model 最小映射（`crates/domain-model`：标准路径前缀表 standard、`validate_domain_path`/`build_observation` mapper，observation_id 为长度前缀无歧义编码并嵌入 collector_session_id）、Poll Engine（`crates/poll-engine`：周期调度/超时/退避重试/取消，PR #9 已合并）、Modbus Driver MVP（`drivers/modbus`：TCP/RTU、地址解析 `1!40001`/`coil:00001`/`input:30001`、批量合并读 FC01~FC04、Native Plugin C ABI v1、超时/断线重连；测试含 mock Modbus TCP server 的 ABI 全链路与 poll-engine 最小集成）、设备管理（`crates/device-manager`：设备实例注册、Driver/Profile 绑定校验、读取项生成与分组、RawReadResult→Profile→Domain→Observation 全链路映射，含 Modbus Mock 全链路测试；测试共用 `crates/modbus-mock`）、数据管道（`crates/data-pipeline`：Telemetry Batch Envelope §31.2——`site_id`/`device_id`/独立批次序号 `sequence`/`message_id` 长度前缀无歧义编码并嵌入 `collector_session_id`、按设备聚合禁止混批、满批+定时刷新输出、保留 Observation 原 `sequence` 不重编号、有界背压/取消/有界排空 `shutdown`、输出关闭终止并异步排空结算、`validate` 拒绝非法与溢出配置；测试 14 项含背压/停机/输出关闭/溢出等，PR #13 已合并）、MQTT 输出（`crates/mqtt-client`：§31/§34/§90.1——rumqttc 0.24 基于 QoS 1 发布、Topic 命名空间 §31.1、PUBACK 结算与断线重发 §31.3/§31.4、指数退避重连 §34.3、Status Envelope `forgelink.status.v1`（在线 `publish_online` retained + 断线按设备全集重建重发周期并优先于普通请求（防饿死）、二次断线已确认设备重新入队、显式离线 `publish_offline` 注销设备、停机前为全部已跟踪设备逐条转发 retained 离线（通道满时循环转发直至期限）、LWT `sent_at_ns=0` 以到达时间为准、断线重排与 rumqttc `EventLoop::clean` 重发顺序严格一致（遗留未重发 -> 在途 pkid 槽位 -> 本会话通道，二次断线 PUBACK 不错位关联）、pkid 碰撞 `Outgoing::AwaitAck` 停放处理（碰撞消息不提前分配标识、旧消息确认后恢复，重发写事件与碰撞恢复写事件区分——碰撞未决断线重连后首个同标识写是重发、不得提前解除停放，第二个未决碰撞立即以 `CollisionOverwritten` 失败结算被覆盖的旧碰撞条目并切换碰撞标识、停放条目按配对标识独立解除，WAL 不提前删除；停机排空三阶段与主循环统一处理碰撞状态）、CONNECT UTF-8 字段与长度校验、TLS/mTLS §90.1、优雅停机 DISCONNECT 不触发 LWT + 排空结算；测试 61 项含 mock broker 的断线重发/多设备在线状态重发/超容量重发推进/重发防饿死/二次断线完整周期/普通消息二次断线确认归属/pkid 碰撞停放与断线恢复/第二个未决碰撞失败结算旧条目/停机排空碰撞结算/离线注销/停机离线/通道满停机离线/背压/停机排空等）、Local Buffer/WAL（`crates/local-buffer`：§103 Embedded DB——SQLite（rusqlite bundled，WAL 日志模式 + synchronous=FULL，崩溃恢复 0 丢失）+ 专用阻塞线程 + 有界异步通道（不阻塞 Tokio）、两级缓冲（内存队列 `memory_records` → 磁盘 `disk_max_bytes`）、以完整 ObservationBatch 为持久化单位、本地递增序号 `local_seq` 按原顺序补传、`message_id` UNIQUE 幂等不覆盖、补传 `replayed=true` 且保留原 message_id/Observation ID/时间、唯一删除路径 `ack`（PUBACK 后），`Closed`/`Disconnected`/`CollisionOverwritten` 均不得删除（`requeue` 放回队头重试）、背压等待中的重复请求与首个请求共享最终落盘结果（未落盘不得提前返回成功）、背压等待队列有界（上限 = memory_records）超限显式报错、同一待落盘记录等待者数量有界（上限 1024，防重试风暴）超限显式报错、恢复分页加载（内存持有 mem+inflight ≤ memory_records，随 next 消耗/ACK 释放按本地序号补充，在途积压期间不补页，磁盘记录数不受内存容量限制且不一次性全量载入）、补传标记为会话内恢复水位（不修改数据库，避免启动时全表 UPDATE 的写入放大）、磁盘容量按估算成本计（payload+topic+message_id+固定开销）、磁盘是第二级容量——内存窗口（memory_records）只限内存持有量非容量，窗口满时 push 仍直接落盘成功（Broker 断网持续写入），由分页加载按序补入内存、Topic 校验与 mqtt-client 一致（§31.1：控制字符/超 65535 字节入队前拒绝）、单条记录成本超过磁盘上限时任何策略立即拒绝（背压等待也永远无法获得容量）、背压等待期间 worker 持续循环处理不阻塞（跨多个分页窗口的过期恢复逐轮清理即可释放容量）且按发送队列最近过期时刻超时唤醒（poll_recv+park_timeout，差值钳制非负防负数转巨大时长超长休眠——saturating_sub 只防溢出不钳负值须显式 max(0)，过期清理失败时短退避重试防空转烧核，到期自动清理释放容量，等待中的请求不被永久阻塞）、next 交付确认握手（结果入回复通道后登记确认，调用方提取后确认；提取前取消——回复或确认通道关闭——记录归还，不滞留 in-flight）、分页失败（load_error）时新记录仅落盘不入内存（next 返回错误而非越过旧记录，不破坏补传顺序）、保留时间到期仅清理发送队列滞留记录（在途记录等待 ACK/requeue 不清理，事务提交成功后才更新内存状态；清理不假设队头时间戳单调，按全部记录过滤+本地序号精确移除）、retention 超 i64 纳秒范围（≈292 年）拒绝、系统时钟异常不 panic、next 失败（反序列化/数据库错误）记录放回队首不丢失、分页加载失败传递给 next（内存耗尽后返回错误而非 None，不误判 WAL 清空）、requeue 多条按本地序号升序插入（不逆转补传顺序）、ACK 先删磁盘成功后再移除在途状态（删除失败重试仍正确扣减容量）、容量不足按 `CapacityPolicy` 显式背压（等待队列）或 `Reject` 报错（禁止静默覆盖，错误单位随容量类型——Memory 为记录条数 / Disk 为字节）、schema v1 完整校验（缺列/列数不符/列类型/NOT NULL/主键/默认值/唯一索引须恰好作用于 message_id 单列/本表 DDL 含 AUTOINCREMENT——全库存在 sqlite_sequence 不得通过，不符一律 Corrupt，user_version=0 但表已存在同样 Corrupt）、有界优雅停机（等待中的背压请求回 `Closed`，已入队记录保留；停机返回成功后不再接受任何命令——接收端原子 close + 已入队命令逐一拒绝，tokio 允许已取得 permit 的发送者在 close 后完成发送，此类竞态窗口内命令的回复通道关闭时按 worker 停机标志映射为 Closed 而非 WorkerFailed）、真实进程强杀 WAL 恢复（测试辅助二进制 `src/bin/wal-crash-helper.rs` 写入落盘后阻塞，父进程 `Child::kill()` 强杀后重开验证：全部恢复/顺序不变/message_id 不重复/replayed=true/原始时间与 Observation ID 保留/ACK 后为空，Windows 句柄释放延迟带重试；就绪等待独立线程读取 + recv_timeout 30s 防挂起（同步 read_line 无限阻塞时超时无效），EOF 即失败，子进程 Drop 守卫兜底 kill+wait 防 panic 遗留，辅助 bin 与 `tests/wal_crash.rs` 集成测试均由 `test-utils` feature 门控——默认 `cargo test --workspace` 不编译（避免干净 target 失败）且不影响 tests/buffer.rs 其余 23 项，tokio `rt` feature 由本 crate 显式声明）；测试 24 项（buffer.rs 23 + wal_crash.rs 1）含顺序/幂等/重启恢复/异常 drop 恢复/requeue 优先/requeue 多条不逆转/磁盘第二级容量（窗口满仍落盘）/磁盘 Reject 与背压/单条超限立即拒绝/背压重复共享结果/背压等待队列有界/分页恢复/分页窗口含在途不补页/等待者数量有界/Topic 控制字符与超长拒绝/过期清理/损坏与非法配置（含 retention 超限）/next 交付前取消归还/背压按保留期限唤醒/坏 schema（版本清零/缺唯一索引/约束不符/唯一索引列不符/非 AUTOINCREMENT/额外列）Corrupt/停机/停机并发命令竞态不 WorkerFailed（1500 任务超通道容量覆盖 permit 竞态）/并发序号单调/真实进程强杀 WAL 崩溃恢复）。`drivers/modbus` 测试加载真实 cdylib 产物，产物缺失时自动 `cargo build -p driver-modbus`（嵌套 build 安全）。尚未完成的主要能力包括 REST 控制链路、Control Engine 和 Edge/Manager 两个运行程序的组装；Collector 运行时已交付（`apps/collector`：§93/§100/§104——配置加载（YAML，§100 校验）、Driver Native Plugin/Profile 目录加载、设备注册与读取项生成、PollEngine→DeviceManager 映射→data-pipeline 组包→local-buffer WAL 落盘→mqtt-client QoS1 发布的只读采集链路（§98），pump/forward/heartbeat 工作任务，有序停机有限排空（停止采集→管道排空→发送循环期限内发完 WAL→MQTT DISCONNECT 结算→缓冲关闭，未确认记录保留重启补传），健康状态快照（设备 last_batch/last_error、MQTT acked/failed、WAL inflight/replayed，§104）；测试 5 项（端到端 2：全链路值映射与 WAL 清空/停机排空不丢数据；韧性 3：断线落盘按序补传、重启 replayed 补传且 message_id 跨会话唯一、停机及时取消采集），共用 `mqtt-client` 的 `test-utils` feature 公开 Mock Broker、`rest-api` 的 `test-utils` feature（REST serve 异常退出模拟钩子 `force_abnormal_exit`）与 `modbus-mock`，cdylib 产物缺失时 Harness 自动嵌套 `cargo build -p driver-modbus`，PR #17 已合并）、REST v1 只读管理接口（`crates/rest-api`：§31.5/§31.6/§104——axum 0.8，设备/资源/属性查询与健康检查端点（schema：`forgelink.devices.v1`/`device.v1`/`resources.v1`/`properties.v1`/`health.v1`）、`forgelink.error.v1` 错误模型（400/404/405/500/503，含 `request_id`）、资源树从属性语义路径派生、有界并发 Semaphore（缺省 64，排队超时 503）、loopback 默认约束与敏感字段不泄漏（§90.1）、优雅停机 2s 限时、404/405 fallback；评审 P2 修复：属性接口含仅可写属性（`readable=false`、`interval_ms` 可为 null 且语义范围 min/max 为 `Value` 外部标签编码）、健康状态反映 MQTT/WAL 异常（`has_anomalies`：设备错误/MQTT last_error/publish 失败/WAL 在途且 MQTT 失败停滞）、`RestApiServer::is_alive` 反映 serve 任务存活（任务退出即不可用）、`shutdown_with_timeout` 统一截止时间有界停机等待（多任务共享一次 grace，超时一次性 abort 全部剩余任务，失败路径不无限挂起）；接入 collector（`rest-api` feature 默认包含、服务器默认禁用——显式 `rest.listen` 才启动，端口 0 为操作系统分配供测试，`CollectorRuntime::rest_addr()` 取实际地址（serve 退出后返回 None）、`CollectorError::Rest` 绑定失败 fail-fast 且启动失败收尾清理已启动组件（限时取消 Scheduler→MQTT/管道结算→缓冲关闭）、REST serve 异常退出通过 `exit_notified` 通知运行时触发错误上报与有序停机（不静默运行）、监督自启动即生效（评审 P1：`run_until_shutdown` 紧跟 `start` 调用——REST serve 异常退出与发送循环异常退出从启动后即刻并行监视并触发有序停机，无需等待外部停机信号；SIGINT/SIGTERM 与上述监视并行监听）、启动竞态防护（评审 P1：订阅前检查 `is_alive`/通知已置位/通道关闭——REST 任务在监督订阅前已异常退出时不得因错过 `watch` 通知而静默采集）、停机第 0 步先关 REST）；评审 P2/P1 修复：属性接口含仅可写属性（`readable=false`、`interval_ms` 可为 null 且语义范围 min/max 为 `Value` 外部标签编码）、健康状态反映 MQTT/WAL 异常（`has_anomalies`：设备错误/MQTT last_error/publish 失败/WAL 在途且 MQTT 失败停滞）、Driver 错误码白名单（评审 P1：`DriverErrorInfo.code` 由 Plugin 任意返回，仅固定集合透传，无法识别映射为 `driver_error`）、`RestApiServer::is_alive` 反映 serve 任务存活（任务退出即不可用）、停机为可共享调用（评审 P1：`shutdown(&self)` 发送停止信号+限时排空，外部持有 `Arc` 副本时运行时停机仍无条件关闭 REST，不把关闭责任转交外部副本；重复调用幂等）、强制 abort 分支显式清除 alive（评审 P2：任务内部置位不执行）、`max_concurrency` 上限校验（评审 P2：超 Tokio `Semaphore::MAX_PERMITS` 返回配置错误，`spawn` 兜底拒绝——0 与超限均返回 `InvalidInput` 不静默修正，启动不 panic）、快照失败日志只记稳定错误码（评审 P2：`StateError` 原文可能含路径/连接信息不落日志）、设备 ID 校验与配置共用 `telemetry_topic`（完整 MQTT 主题长度同集合，§31.1）、非法 URL 编码统一映射 §31.6 400 错误载荷（PathRejection 含 request_id，评审 P2）；测试 39 项（rest-api 30 单测 + collector 集成 7 + poll-engine 3：端到端全端点/仅可写属性可见/健康降级/404 错误模型/敏感字段/控制路由 404/并发不阻塞采集/停机端口关闭/默认不启用/REST 绑定失败清理后启动报错/设备标识空格反斜杠可查询/错误消息不泄漏内部细节（设备失败与 MQTT 断线失败时 REST last_error 只含稳定错误码，原始文本只进脱敏日志）/REST 异常退出启动后立即触发有序停机（无外部停机信号）/正常停机不触发异常通知/停机超时强制 abort 清除存活标记/非法 URL 编码统一 400 错误载荷/设备 ID 完整主题长度校验/多任务统一截止时间有界停机/外部持有 REST 句柄仍无条件关闭（停机可共享调用且幂等）/监督启动前已异常退出立即触发停机（启动竞态回归）））。仓库已推送至 GitHub（origin，默认分支 `main`）；GitHub Actions CI 见 `.github/workflows/ci.yml`。
- 唯一架构依据是 `Rust工业IoT采集平台架构设计方案.md`，提出架构或编写代码前必须阅读相关章节。
- 日常编码、测试和变更要求遵循 `开发规范.md`。
- 文档是渐进式设计：冲突时以后文及“更新后”“最终”章节为准；仍无法判断时先询问，不自行创造第三种方案。
- 未经用户明确要求，不初始化代码、CI 或部署文件。

## 固定架构

- 五层边界固定：`Transport` -> `Protocol Driver` -> `Device Profile` -> `Domain Model` -> `Observation`。
- Driver 表示协议，不表示设备型号。仅当报文、握手、寻址或认证机制本质不同才拆 Driver；型号、地址、缩放、单位、枚举和能力差异属于 Profile。兼容型号可共用 `Profile Family`。
- Core 不得按 `driver_id` 分支，只能通过 Driver Manager 分发。
- Driver 地址是私有不透明数据；Core 只传递 `DriverReadItem { id, address, expected_type }`（语义 Property -> Driver 地址的映射由 Profile 完成）。
- 批量读取、地址合并和会话串行化属于 Driver，不属于 Core。
- Driver 返回原始结果，`Observation` 只能由 Profile + Domain 映射后生成；每个 `Observation` 必须包含 Quality 和 Timestamp。
- Property Write 与 Command 必须分开，并统一经过 Control Engine，完成认证、授权、参数与前置条件校验、风险策略、设备级队列、超时/取消/去重、`request_id` 关联和审计。
- 软件前置条件不能替代安全 PLC、急停、门锁等硬件安全机制。
- Runtime Role（collector/edge/manager）与 domain/driver/profile 正交。Collector 通过 Cargo feature 禁用控制，并设置运行时 `read_only`；只读版本不得暴露控制入口。
- Driver 支持 Static、Native Plugin、Process Plugin。Native Plugin 仅使用稳定 C ABI：`cdylib`、`forgelink_driver_entry_v1()`、ABI 版本校验和 `libloading`；禁止跨 FFI 暴露 Rust trait 或 `async fn`。Vendor SDK 优先使用 Process Plugin 隔离。
- 目标平台：Windows x64、Linux x64、Linux ARM64。
- MVP：Modbus、Poll Engine、MQTT 输出、Local Buffer、REST API 只读（已交付）；REST 控制链路（Control Engine、driver-write）待交付；S7、CIP、FOCAS 等按文档阶段推进。

## 实施约定

- 规范模型和命名以文档定义为准：`Device`、`Resource`、`Property`、`Observation`、`Value`、`Quality`、`CommandRequest/Result`、`RawReadResult`、`DriverReadItem`、`DriverApiV1` 等不得擅自改名或改变字段。
- 新增设备前先判断：已有协议则新增 Profile；新协议才新增 Driver；随后映射 Domain Model。
- 顶层规划为 `crates/`、`drivers/`、`profiles/`、`apps/`；具体 workspace 组成按文档后部最终方案执行。
- 新增或修改架构文档使用中文，并检查术语、章节编号、交叉引用和代码示例一致性。
- 代码应有充分且合理的注释；公共 API 必须使用 Rustdoc，详细要求见 `开发规范.md`。
- 网络 I/O、采集调度和消息管道应合理使用 Rust 异步机制，并采用有界并发、背压和阻塞任务隔离；不得无限制创建异步任务。
- 工具链：Rust 1.95+，edition 2024。构建验证命令：`cargo check --workspace`（全部 19 个成员）。当前 CI 实际覆盖 `cargo fmt --all --check`、`cargo check --workspace`、`cargo test --workspace`、`cargo test -p local-buffer --all-features --test wal_crash`（真实进程强杀 WAL 崩溃恢复，test-utils feature 门控）、`cargo doc --workspace --no-deps`；带 `--all-targets --all-features` 的完整检查和 Clippy 由开发规范要求在本地或 CI 增强任务中执行。
- Collector 的 `control` feature 目前为空占位标记；接入控制链路（control-engine、driver-write）后，必须验证 `cargo check -p collector --no-default-features --features collector` 的构建产物不包含控制代码。
- Git 操作必须遵循 `开发规范.md`：禁止直接在 `main` 分支开发、提交或推送，所有变更通过独立分支和 Pull Request 合并。
- 保持任务范围最小，不重新讨论文档中已确定的决策。

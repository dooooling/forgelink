[package]
name = "s7comm-mock"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# 同步 Mock S7comm Server（测试共用，非生产代码）：
# ISO-on-TCP（TPKT/COTP）握手、Setup Communication 协商、Read/Write Var
# 应答与内存映像应用、行为开关（延迟/断连/概率超时/逐项拒绝/失步注入）
# 与请求捕获统计。仅用于测试，不参与生产构建路径。

[dependencies]
serde_json = "1"
